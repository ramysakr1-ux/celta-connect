# Handoff: Progress Record naming fix + remaining resolutions

## Resolved this session, corrected against the CELTA 5 source
- **Naming**: use "Progress Record — Stage 1/2/3" verbatim (the CELTA 5 booklet's own heading), not "Progress Report" — corrects an earlier wrong guess. Never shorten Cambridge's "Appeal Stage One/Two" below that. Apply everywhere "Stage 1/2/3" appears in UI copy (Progress tab, Roster, announcements, unified tracking columns).
- **Application-form consent**: one checkbox, every applicant (not just rejected ones) — file kept at the centre regardless of outcome. Separate from the five course-commitments acknowledgements (those are post-acceptance only).
- **Triage notification routing**: "clear problems" lane goes to whoever's configured as interviewer for that course/intake, least-loaded this intake, admin can override to a named person. In-app flag, never a push.
- **Assessor portfolio count & observation link**: flat minimum of four plus fixed categories (not size-scaled); same link trainees use; MCT decides which candidates manually.

## Files
- `admissions-and-close-out.md`
- `for-claude-code-admissions.md`
- `for-claude-code-assessor-interface.md`
