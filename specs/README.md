# Handoff: 16 Aug session resolutions

## Overview
Everything decided and specced in this session — blocking-decision answers, spec corrections, and one new buildable item (the getting-started doc). None of this has been sent before.

## New to build
**Getting-started guide, single-page combined doc** — `for-claude-code-getting-started-doc.md`. Linked from the staff invitation email. Combines `Admin Task Guides.dc.html`'s seven existing guides into one flowing, standalone page (no app chrome, no login needed) — build as a `doc_page`, not the in-app card UI.

## Corrections to specs already sent (re-send these files — they replace earlier copies)
1. **Pre-course task delivery** — resolved: self-check inside Connect via the welcome-email login link, never emailed as content. (`review-notes.md`)
2. **Rejected-applicant retention** — resolved and corrected: same one-week grace/erasure clock as everyone else; Connect never holds six months, that's the centre's own responsibility on their Drive. This corrects an earlier draft line that had Connect holding records for six months. (`build-spec.md`)
3. **"Connect" vs "CELTA Connect" naming** — confirmed already resolved (locked 7 Aug spec was right, the "still open" flag was stale). (`review-notes.md`)
4. **Progress tab houses Stage 1/2/3** — confirmed yes, with exact trigger logic (Stage 1: report mandatory/tutorial optional; Stage 2: both mandatory; Stage 3: report mandatory only if triggered, tutorial optional). (`for-claude-code-progress-tab.md`)
5. **Read-only timetable view** — was incorrectly flagged deferred; a full spec already exists (`for-claude-code-timetable-view.md`) and should be built from directly.
6. **Assessor portfolio count & observation link** — resolved: portfolio count is a flat minimum of four plus fixed categories, not size-scaled; the observation link is the same link trainees already use, and which candidates are observed is decided manually by the MCT in advance. (`for-claude-code-assessor-interface.md`)
7. **Staff invitation email** — now specified to carry the getting-started doc link. (`for-claude-code-email-inventory.md`)

## Still genuinely open — needs a decision before building
- **Which tutor gets notified for the "clear problems" pre-interview triage lane** — proposed: whoever's configured as interviewer for that course/intake, same least-loaded/override rule as interview booking. Not yet confirmed.
- **True part-time course structure** (distinct from the 5-week-Fridays-off stretch, which is already settled) — 10-week vs 12-week variants, deferred on purpose, cross that bridge later.

## Files in this package
- `for-claude-code-getting-started-doc.md` (new)
- `review-notes.md`
- `build-spec.md`
- `for-claude-code-progress-tab.md`
- `for-claude-code-timetable-view.md`
- `for-claude-code-assessor-interface.md`
- `for-claude-code-email-inventory.md`
- `for-claude-code-session-changelog-2026-08-15.md`
