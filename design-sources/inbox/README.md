Drop zone for design handoffs.

Desktop and Downloads are blocked to me by macOS, so files have to land
inside the project. This folder is deliberately kept empty -- a drag that
goes the wrong way here costs nothing.
