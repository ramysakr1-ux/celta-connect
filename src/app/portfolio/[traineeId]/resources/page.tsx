// Resource Hub (§5) -- placeholder until that phase is built.
export default function ResourceHubPage() {
  return (
    <div className="sheet p-6 text-sm text-muted">Resource Hub -- coming in §5.</div>
  );
}
